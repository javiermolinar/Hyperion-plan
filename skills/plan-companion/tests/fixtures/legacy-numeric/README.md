These frozen fixtures were generated with the immutable, reviewed Python Plan Companion implementation on 2026-09-21. They include its actual Markdown, sidecar fingerprints, approved request receipt, expected plan, and canonical JSON number encodings. They exercise integral floats, exponents, negative zero, subnormals, large finite floats, and integers beyond JavaScript's safe range. Tests consume this data without requiring Python or regenerating expectations from the implementation under test.
